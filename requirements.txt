# Streamlit Cloud requirements — only what app.py needs.
# The full ML pipeline stack is in requirements_pipeline.txt (run locally).

streamlit>=1.28.0,<2.0.0
Pillow>=10.0.0
plotly>=5.15.0
requests>=2.31.0
